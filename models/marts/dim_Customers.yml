version: 2

models:
  - name: dim_Customers
    description: Dimensão para identificar os clientes
    tests:
      - dbt_utils.equal_rowcount:
          compare_model: ref('stg_AdventureWorks_Customers')
    columns:
      - name: customer_sk
        tests:
          - unique
          - not_null
        description: Surrogate Key
      - name: customer_id
        description: Natural Key
  
      - name: rowguid
      - name: modifiedDate